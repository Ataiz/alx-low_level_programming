#include <stdio.h>
#include "main.h"
/**
 * mul - mul block
 * Description: Write a function that multiplies two integers.
 * Prototype: int mul(int a, int b);
 *
 * @a: int type
 * @b: int type
 *
 * Return: c, which is the multiplication of a and b
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
